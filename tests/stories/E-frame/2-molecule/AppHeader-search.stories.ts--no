import { AppHeader-search } from '$ui/index.js';
import type { Meta, StoryObj } from '@storybook/svelte';
const meta = {
	title: 'C - molecule/AppHeader-search',
	component: AppHeader-search,
	tags: ['autodocs']
} satisfies Meta<AppHeader-search>;
export default meta;
type Story = StoryObj<typeof meta>;
export const Primary: Story = {
	args: {
		// active: ''
	}
};
