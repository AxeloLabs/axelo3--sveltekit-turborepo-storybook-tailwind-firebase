// xport const csr = true;
// xport const prerender = true;
export const ssr = false;

// car on ne peut pas pre-render les ID-dynamic, j ignore leurs valeurs (comme tous les nopms des accounts), POUR LINSTANT
export const prerender = 'auto';
